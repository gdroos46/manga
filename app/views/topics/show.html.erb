
<p id="notice"><%= notice %></p>

<h1 id="title"><%= @topic.topic_title %></h1>

<%= render "form", comment: @comment %>



<table>
  <thead>
    <tr>
      <th>TopicTitle</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody id="task-list">
    <% @topic.comments.each do |comment| %>
      <tr>
        <td><%= comment.text %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: '削除しますか?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Comment', "/topics/#{@topic.id}/comments/new" %>